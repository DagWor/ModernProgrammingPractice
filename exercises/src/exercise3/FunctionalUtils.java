package exercise3;

import java.util.function.BiFunction;
import java.util.*;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public abstract class FunctionalUtils {
//    public static BiFunction<Tournament, Integer, List<Player>> topScorer = (tournament, k) ->
//            Stream.of(tournament)
//                .flatMap(tournament1 -> tournament1.getMatchList().stream())
//                .map(Match::getStats)
//                .flatMap(stats -> stats.getStatList().stream())
//                .filter(stat -> stat.ge() == StatType.GOAL)
//                .sorted((player1, player2) -> player2.getGoalList().size() - player1.getGoalList().size())
//                .limit(k)
//                .collect(Collectors.toList());



}
